{
    "extends": "next/core-web-vitals",
    "rules": {
    // turn *any* console.log into an error; still allow warn() and error()
      "no-console": ["error", { "allow": ["warn", "error"] }]
    }
  }